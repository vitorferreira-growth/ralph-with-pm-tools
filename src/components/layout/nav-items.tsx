'use client'

import type { ReactNode } from 'react'

export function NavItems(): ReactNode {
  // A ser implementado em Step 3.2
  return <nav className="space-y-sm">{/* Navigation items */}</nav>
}
