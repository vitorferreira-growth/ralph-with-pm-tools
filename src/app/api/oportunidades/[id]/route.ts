import { NextResponse } from 'next/server'

// GET /api/oportunidades/:id - Busca oportunidade
export async function GET(): Promise<NextResponse> {
  // A ser implementado em Step 7.1
  return NextResponse.json({ message: 'API de oportunidades - a ser implementada' }, { status: 501 })
}

// PUT /api/oportunidades/:id - Atualiza oportunidade
export async function PUT(): Promise<NextResponse> {
  // A ser implementado em Step 7.1
  return NextResponse.json({ message: 'API de oportunidades - a ser implementada' }, { status: 501 })
}

// PATCH /api/oportunidades/:id - Atualiza stage (kanban)
export async function PATCH(): Promise<NextResponse> {
  // A ser implementado em Step 7.1
  return NextResponse.json({ message: 'API de oportunidades - a ser implementada' }, { status: 501 })
}

// DELETE /api/oportunidades/:id - Remove oportunidade
export async function DELETE(): Promise<NextResponse> {
  // A ser implementado em Step 7.1
  return NextResponse.json({ message: 'API de oportunidades - a ser implementada' }, { status: 501 })
}
