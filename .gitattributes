# Git attributes for better diffing and security

# Ensure LF line endings
* text=auto eol=lf

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
*.tgz binary

# Security: Mark sensitive files as binary to prevent accidental text diffs
*.key binary
*.pem binary
*.p12 binary
*.jks binary
*.credentials binary

# Large files that should use Git LFS
*.db filter=lfs diff=lfs merge=lfs -text
*.sqlite filter=lfs diff=lfs merge=lfs -text
*.dump filter=lfs diff=lfs merge=lfs -text

# Language-specific settings
*.py diff=python
*.js diff=javascript
*.json diff=json
*.md diff=markdown
*.yaml diff=yaml
*.yml diff=yaml

# Generate better conflict markers
*.py merge=union
*.js merge=union
*.json merge=union